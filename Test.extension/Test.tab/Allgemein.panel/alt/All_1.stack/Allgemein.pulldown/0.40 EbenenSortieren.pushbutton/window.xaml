<Window 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EbenenSortieren" Height="350" Width="575"  ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" ScrollViewer.VerticalScrollBarVisibility="Disabled" HorizontalContentAlignment="Center"  Background="SkyBlue">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="202*"/>
            <RowDefinition Height="30*"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="dataGrid" ItemsSource="{Binding}" AutoGenerateColumns="False" Grid.Row="0" Background="LightBlue" RowBackground="WhiteSmoke"  CanUserAddRows="False" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ebenen" Binding="{Binding Ebene}" Width="190" IsReadOnly="True"/>
                <DataGridTextColumn Header="Abkürzung" Binding="{Binding Abk}" Width="187"/>
                <DataGridTextColumn Header="Nummer" Binding="{Binding Nr}" Width="190"/>
                </DataGrid.Columns>
                </DataGrid>
        <Button Content="OK" Grid.Row="1" FontSize="16" Click="ok" Margin="358,3,45,3"/>
        <Button Content="Cancel" Grid.Row="1" FontSize="16" Click="cancel" Margin="45,3,358,3"/>

    </Grid>
</Window>